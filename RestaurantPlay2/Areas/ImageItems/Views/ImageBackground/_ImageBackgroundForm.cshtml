@using RestaurantPlay2.Areas.ImageItems.ViewModels.BackgroundImage
@model SaveBackgroundImage

@using(Ajax.BeginForm("SaveBackgroundImage", "ImageBackground", new AjaxOptions
{
    InsertionMode = InsertionMode.Replace,
    UpdateTargetId = "images",
    HttpMethod = "POST"
}, new {enctype = "multipart/form-data"}
))
{
    @Html.ValidationSummary(true)

    <div class="form-horizontal">
        <div class="form-group">

        </div>
    </div>
}
